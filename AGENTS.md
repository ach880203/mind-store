\# 전역 에이전트 규칙 (AGENTS.md)



\## 0. 최우선 원칙 (반드시 준수)

\- 화면(UI)에 표시되는 모든 문구는 \*\*반드시 한글\*\*이어야 한다.

\- \*\*한글이 깨지지 않도록\*\* 인코딩/출력/폰트/응답 헤더 등 관련 요소를 항상 점검한다.

\- 이미 한글로 작성된 텍스트(라벨/문구/메시지/주석/문서)는 \*\*절대 영어로 바꾸지 않는다.\*\*

\- 기존에 존재하는 주석은 \*\*절대 삭제하지 않는다.\*\* (수정도 최대한 지양, 필요 시 옆에 보완 주석 추가)



\## 1. 코드 작성 스타일 (초보 친화 + 세련됨)

\- 변수/함수/컴포넌트 이름은 역할이 바로 보이게 짓는다. (과한 축약 금지)

\- 변경은 “최소 단위”로 쪼개고, 변경 이유/영향 범위를 함께 설명한다.

\- 예외/경계값(빈 값, null, 네트워크 실패 등)은 반드시 고려한다.

\- 정리(리팩터링)로 코드가 짧아져도 \*\*읽기 어려워지면 금지\*\*한다.

\- 정리(리팩터링)로 코드가 짧아져도 \*\*읽기 어려워지면 지양\*\*하지만 자세한 주석으로 설명을 기입할 시 사용 가능하다.



\## 2. 주석 규칙 (코드 리뷰 도움 최우선)

\- 기존 주석: \*\*절대 삭제 금지\*\* 예외로 틀린 주석의 경우 수정해준다.

\- 새 주석: 아래를 충족해야 한다

&nbsp; - “왜 이렇게 했는지(의도)”를 설명한다.

&nbsp; - 초보 기준으로 용어를 풀어서 설명한다.

&nbsp; - 변경/주의 포인트(깨질 수 있는 부분)를 구체적으로 남긴다.

\- 주석 언어: \*\*한글\*\*



\## 3. 한글 깨짐 방지 체크리스트 (작업 시 항상 확인)

\- 소스 파일 저장 인코딩이 UTF-8인지 확인한다.

\- (백엔드) 응답 `Content-Type`에 `charset=utf-8`이 적절히 포함되는지 확인한다.

\- (DB) 문자셋/정렬(collation)이 한글에 안전한 설정인지 확인한다.

\- (프론트) 폰트 로딩/번들/JSON 직렬화 과정에서 한글이 변형되지 않는지 확인한다.



\## 4. 작업 보고 방식

\- 답변/변경 제안은 다음 순서로 제공한다:

&nbsp; 1) 변경 목표(한글로)

&nbsp; 2) 핵심 변경 요약(한글로)

&nbsp; 3) 코드/설명(한글 주석 포함)

&nbsp; 4) 확인 방법(실행/테스트/재현 절차)

